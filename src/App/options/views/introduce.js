import React, { useState, useEffect } from "react";
import { Layout, Title } from "../components/styleComponent";

const Intro = (props) => {
	const { title } = props;

	return (
		<Layout>
			<Title>{title}</Title>
			asda
		</Layout>
	);
};

export default Intro;
